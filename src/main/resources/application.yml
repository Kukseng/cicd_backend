
keycloak:
  realm: sq-api

app:
  default-role: PARTICIPANT

spring:
  application:
    name: stackquiz-api

  threads:
    virtual:
      enabled: true

  servlet:
    multipart:
      max-request-size: 20MB
      max-file-size: 10MB

  datasource:
    #    url: jdbc:postgresql://dpg-d2inormr433s73e0p820-a.oregon-postgres.render.com:5432/stack_quiz_db
    #    username: stack_quiz_db_user
    #    password: B30kvFA40SWgA3YQd2HkjPjDTkiU1srO
    #    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:4000/bankdb
    username: postgres
    password: seng4321
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

    properties:
      hibernate:
        format_sql: true


  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 60000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
        shutdown-timeout: 100ms


  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://keycloak-prod.rattanakmony.com/realms/stackquiz